# sm_profile

`sm_profile` manages ShakeMap profiles. Profiles allow a single user to
maintain multiple ShakeMap installations on the same machine, each with its
own configuration and data directories. Profile information is stored in
`~/.shakemap/profiles.conf` by default.

## Usage

```{program-output} sm_profile -h
```