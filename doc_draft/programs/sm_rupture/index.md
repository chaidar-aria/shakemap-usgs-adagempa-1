# sm_rupture

`sm_rupture` creates a ShakeMap 4 `rupture.json` file. It can convert a
ShakeMap 3 `*_fault.txt` file, parse USGS NEIC or FSP finite fault formats,
or interactively prompt the user to enter rupture geometry manually.

## Usage

```{program-output} sm_rupture -h
```