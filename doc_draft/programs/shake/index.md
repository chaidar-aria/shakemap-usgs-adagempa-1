# shake

The `shake` command runs one or more ShakeMap processing modules for a given
event.

## Usage

```{program-output} shake -h
```

## Notes

The list of available modules shown above reflects the modules installed in
the current ShakeMap environment. The set of modules may vary depending on
the configuration.

To get help on a specific module, run:

```
shake help <module>
```